@using Microsoft.AspNetCore.Localization
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor

@{
    var culture = HttpContextAccessor.HttpContext?.Features
        .Get<IRequestCultureFeature>()?.RequestCulture.Culture.TwoLetterISOLanguageName ?? "en";
}

<!DOCTYPE html>
<html lang="@culture">
<head>
    <meta charset="utf-8" />
    <!-- ✅ makes site scale correctly on mobile -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <base href="/" />
    <link rel="stylesheet" href="app.css" />
    <link rel="stylesheet" href="OC.LUAC.UiLayer.styles.css" />
    <link rel="stylesheet" href="css/output.css" />   <!-- Tailwind CSS -->
    <link rel="icon" type="image/png" href="/images/ICON_LOGO.png" />

    <!-- Flatpickr styles -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css" />

    <HeadOutlet @rendermode="InteractiveServer" />
</head>
<body class="overflow-x-hidden">
    <!-- ✅ stops white bars -->
    <Routes @rendermode="InteractiveServer" />

    <!-- Culture init -->
    <script>
        (function () {
            var culture = localStorage.getItem('culture');
            if (!culture) return;

            // persist for 1 year, lax, secure on https
            var oneYear = 60 * 60 * 24 * 365;
            var cookie = ".AspNetCore.Culture=c=" + culture + "|uic=" + culture
                + "; path=/; samesite=lax; max-age=" + oneYear;
            if (location.protocol === "https:") cookie += "; secure";
            document.cookie = cookie;

            // best-effort hint to the client side
            try {
                Object.defineProperty(navigator, "language", { value: culture, configurable: true });
                Object.defineProperty(navigator, "languages", { value: [culture], configurable: true });
            } catch { /* ignore */ }
        })();
    </script>

    <!-- Flatpickr init helper -->
    <script>
        window.initFlatpickr = function () {
            if (!window.flatpickr) return;
            var start = document.querySelector("#startDate");
            if (start && !start._flatpickr) {
                flatpickr(start, { dateFormat: "Y-m-d" });
            }
            var end = document.querySelector("#endDate");
            if (end && !end._flatpickr) {
                flatpickr(end, { dateFormat: "Y-m-d" });
            }
        };
    </script>

    <!-- Flatpickr script -->
    <script src="https://cdn.jsdelivr.net/npm/flatpickr" defer></script>

    <!-- 🔑 Your UI auth helpers (uiLogin, uiAdminLogin, uiLogout) -->
    <script src="/app.js"></script>

    <script src="_framework/blazor.server.js"></script>
</body>
</html>
